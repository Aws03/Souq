bin/
obj/
node_modules/
dist/
*.user
.DS_Store
.vs/
.vscode/
# أسرار Docker Compose المحلية (كلمة مرور SQL، مفتاح JWT، مفاتيح Stripe) — .env.example فقط يُرفع.
.env
# صور المنتجات المرفوعة (محتوى مستخدم، لا مصدر)
src/Souq.API/wwwroot/uploads/

# نتائج تشغيل Playwright (لقطات وآثار) — لا تُلتزم.
frontend/test-results/
frontend/playwright-report/
