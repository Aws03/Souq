import { afterEach } from 'vitest';
import '@testing-library/jest-dom/vitest';

// ============================================================================
// تنظيف ما عُرض بين اختبار وآخر. مكتبة الاختبار تسجّله تلقائياً حين تكون دوالّ الاختبار
// عامّة (globals)، وهي ليست كذلك هنا عمداً — فيُسجَّل صراحةً.
//
// الاستيراد مشروط بوجود مستند: أغلب اختبارات هذا المشروع منطق خالص تعمل في بيئة node
// الأسرع، واستيراد مكتبة عرض هناك يحمّل 110 اختبارات ثمن ما لا تستعمله.
// ============================================================================
if (typeof document !== 'undefined') {
  const { cleanup } = await import('@testing-library/react');
  afterEach(cleanup);

  // حزمة الترجمة تُحمَّل للغة الزائر وحدها (استيراد ديناميكي)، فالتهيئة صارت غير متزامنة.
  // انتظارها هنا مرّة واحدة يعفي كل اختبار مكوّن من انتظارها — وبلا ذلك يعرض `t` اسم
  // المفتاح بدل نصّه فتسقط الاختبارات على شيء لا علاقة له بما تفحصه.
  await (await import('../i18n')).i18nReady;
}
