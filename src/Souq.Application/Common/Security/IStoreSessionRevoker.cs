namespace Souq.Application.Common.Security;

// ============================================================================
// إبطال جلسات متجر كاملةً — كل حساباته، من جهة المنصّة (TD-66).
//
// **لماذا منفذ لا مستودع؟** لأن هذا العمل يخرق قاعدةً قائمة عن قصد: `User` و`RefreshToken`
// مُرشَّحان بنطاق الطلب (`ITenantOrPlatformOwned`)، ومعالجُ المنصّة يعمل في نطاق المنصّة — فلا يرى
// من حسابات المتجر شيئاً أصلاً. فالتنفيذ يلزمه تجاوز المرشّح بشرط متجرٍ مكتوب بيد، وكتابةٌ
// مجمّعة، وكلاهما محصور في قائمة مُراجَعة في `TenancyRuleTests`. المنفذ يجعل ذلك **قراراً واحداً
// في مكان واحد** بدل تجاوزٍ يتناثر.
//
// **ومتى يُنادى؟ عند الأرشفة وحدها.** قرار المالك C-17 = B: الإيقاف يُبقي التاجر داخلاً ليُصلح سبب
// الإيقاف، فإبطال جلسته عنده يناقض القرار نفسه. الأرشفة نهائية، وهي الحالة التي كان فيها العطب:
// نقاط الإدارة تردّ 503 للمؤرشف، لكن `/api/auth/refresh` **مفتوحة له عمداً**، فكانت جلسات إدارته
// قابلةً للتجديد إلى الأبد.
// ============================================================================
public interface IStoreSessionRevoker
{
    // يعيد عدد الحسابات التي دُوِّر ختمها. `reason` يُكتب على رموز التجديد للتشخيص — لا سرّ فيه.
    Task<int> RevokeAllAsync(int tenantId, string reason, CancellationToken ct = default);
}
