namespace Souq.Application.Common.Interfaces;

// ============================================================================
// إشاراتُ إبطالِ الذاكرات المؤقتة بين نسخ الخادم (C4، [ADR-0057](0057)).
//
// **ما تفعله هذه الواجهة هو النشر والقراءة فقط.** مَن يُبطل يُبطل محلّياً في الحال — فالنسخةُ
// التي كتبت التغيير يجب أن تراه فوراً — **ثمّ** ينشر إشارةً تصل إلى الباقيات في دورةِ استطلاع.
// الترتيبُ مقصود: النشرُ أوّلاً ثمّ الإبطال المحلّيّ كان سيترك نافذةً تخدم فيها هذه النسخةُ نفسُها
// قيمةً قديمة.
// ============================================================================
public interface ICacheSignals
{
    // يزيد جيلَ الإشارة بجملةٍ ذرّية. يُنادى **بعد** الحفظ: إشارةٌ تسبق التزاماً قد تُعلن تغييراً
    // لم يقع.
    Task BumpAsync(string signal, CancellationToken ct = default);

    // كلُّ الأجيال في نداءٍ واحد، للمراقب.
    Task<IReadOnlyDictionary<string, long>> ReadAllAsync(CancellationToken ct = default);
}
