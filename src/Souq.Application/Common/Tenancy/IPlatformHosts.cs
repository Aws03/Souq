namespace Souq.Application.Common.Tenancy;

// ============================================================================
// مضيفو المنصّة (ADR-0006): المضيفون الذين يُحلّون إلى منطقة المنصّة لا إلى متجر. الإعداد يعيش في طبقة API
// (TenancyOptions، ومعه مضيف التطوير admin.localhost)، والسؤال يُطرح هنا عند ربط نطاق بمتجر.
//
// لماذا يُسأل؟ لأن تفرّد النطاقات كان يُفحص بين المتاجر وحدها. ربط مضيف المنصّة بمتجر لا يسرق شيئاً —
// التحديد يسأل عن مضيفي المنصّة أوّلاً فيبقى المضيف للمنصّة — لكنه يُنتج متجراً نطاقه الأساسي لا يصل إليه
// أبداً، ودعوةَ مديرٍ برابط يُفتح على مضيف المنصّة. خطأ إعداد صامت، يُرفض عند مصدره.
// ============================================================================
public interface IPlatformHosts
{
    bool IsPlatformHost(string host);
}
