namespace Souq.Application.Features.Billing;

// ============================================================================
// سحبُ الرقم التالي في سلسلةِ مستندات المنصّة (C5، [ADR-0056](0056)) — نظيرُ `IOrderNumbers`
// بالضبط، ولنفس السبب: الزيادةُ الذرّية فعلُ قاعدةٍ داخل معاملة، والمجالُ لا يعرف معاملات.
//
// **ويُستدعى داخل معاملة الإصدار وحدها.** القفلُ الذي يأخذه التحديثُ يبقى حتى الالتزام، فإصدارٌ
// متزامنٌ ينتظر لحظةً ويأخذ الرقم التالي؛ ورقمُ معاملةٍ أُلغيت يعود معها فلا تُترك فجوة.
// ============================================================================
public interface IPlatformDocumentNumbers
{
    // الرقمُ التالي في السلسلة، مُنسَّقاً ببادئته: `INV000042`. البادئةُ من إعداد فوترة المنصّة
    // فتصل وسيطاً — لا يقرأ المنفذُ إعداداً، ولا يُكتب رمزٌ ثابت في تنفيذه.
    Task<string> NextAsync(string series, string prefix, CancellationToken ct);
}
