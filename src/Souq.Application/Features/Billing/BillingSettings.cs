namespace Souq.Application.Features.Billing;

// ============================================================================
// إعدادات وحدة Billing (C2). اليوم بندٌ واحد: دورة مصالحة عدّادات الحصص.
//
// القسم `Billing` في الإعداد. صفر يعطّل المنسّق — والاختبارات تعطّله كي لا تسابق دورةٌ خلفية
// ما تفحصه، وهي نفس القاعدة التي تتبعها بقيّة المنسّقات هنا.
// ============================================================================
public sealed class BillingSettings
{
    // ============================================================================
    // ستّ ساعات افتراضاً. المصالحة ليست دفاعاً بل شبكة أمان: المسارات نفسها تحجز وتُطلق صحيحاً،
    // وهذه تلتقط ما نسيه مسارٌ أو غيّرته يدٌ في القاعدة. فدورةٌ أكثر تواتراً تشتري قليلاً وتكلّف
    // عدّاً كاملاً لكل حدّ لكل متجر — والعدّ هو بالضبط ما بُني العدّاد كي لا يُفعَل في كل طلب.
    // ============================================================================
    public int QuotaReconcileIntervalMinutes { get; set; } = 360;

    // ============================================================================
    // دورةُ المطالبة (C6، ADR-0058). ستّون دقيقةً افتراضاً: السلّمُ يُقاس بالأيام، فدورةٌ أكثر
    // تواتراً لا تُقرّب تعليقاً ولا تذكيراً بشيءٍ يُذكر — وتقرأ ما استحقّ عبر المتاجر كلَّ مرّة.
    //
    // وصفرٌ يعطّل المنسّق، كبقيّة المنسّقات هنا. **وهو ليس مفتاحَ تفعيل المطالبة**: ذاك
    // `PlatformBillingSettings.DunningEnabled`، ويعيش في القاعدة لأنّه قرارٌ تجاريّ يملكه
    // المشغّل — لا في ملفّ إعدادٍ يملكه من ينشر.
    // ============================================================================
    public int DunningSweepIntervalMinutes { get; set; } = 60;
}
