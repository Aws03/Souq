namespace Souq.Domain.Enums;

// ============================================================================
// لماذا Enum وليس string؟ (مبدأ: اجعل الحالات غير الصحيحة مستحيلة)
// حالات الطلب محدودة ومعروفة مسبقاً. باستخدام enum نمنع أي قيمة عشوائية
// مثل "shippeddd". المترجم نفسه يحرس القيم الصحيحة لنا وقت كتابة الكود.
// الترتيب يعكس دورة حياة الطلب الطبيعية.
// ============================================================================
public enum OrderStatus
{
    Pending = 0,    // بانتظار الدفع
    Paid = 1,       // تم الدفع
    Shipped = 2,    // تم الشحن
    Delivered = 3,  // تم التسليم
    Cancelled = 4   // ملغى
}
