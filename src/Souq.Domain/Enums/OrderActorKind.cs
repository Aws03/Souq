namespace Souq.Domain.Enums;

// من غيّر حالة الطلب (المرحلة 9) — يُخزَّن رقماً في سجلّ الحالة. الترتيب ثابت (قيم مخزّنة).
public enum OrderActorKind
{
    System = 0,          // منسّق خلفي (انتهاء المهلة) أو مسار داخلي (فشل بدء الدفع)
    Customer = 1,        // صاحب الطلب
    Staff = 2,           // مدير أو موظّف متجر، بمعرّف حسابه
    PaymentGateway = 3,  // دفع أكّدته البوّابة أو رفضته
}
