namespace Souq.Domain.Common;

// ============================================================================
// أدوار المستخدمين. ثوابت نصّية عمداً لا enum: تفويض ASP.NET Core
// (Authorize(Roles=...)) ومطالبات JWT نصّية في جوهرها، فالـ enum يفرض تحويلاً
// عند كل حدّ دون فائدة حقيقية. الثوابت تمنع الخطأ الإملائي — وهو الخطر الفعلي —
// وتبقى المصدر الوحيد لأسماء الأدوار في كل الطبقات.
// ============================================================================
public static class Roles
{
    public const string Customer = "Customer";
    public const string Admin = "Admin";
}
