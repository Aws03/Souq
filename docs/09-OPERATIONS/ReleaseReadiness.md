# Release readiness

> **What this page is:** every known blocker between Souq today and a first paying customer, triaged by severity, with the evidence that it is real, what closing it takes, and what proves it closed. It is the page to read before saying "we can go live".
> **How it differs from its neighbours:** [RiskRegister.md](../02-ARCHITECTURE/RiskRegister.md) says *how this system can hurt someone*; [TechnicalDebt.md](../12-ROADMAP/TechnicalDebt.md) says *what we chose to postpone*; this page says **what stops a release**, and tracks each item to closed.
> **Checklist for the release itself:** [ProductionReleaseChecklist.md](ProductionReleaseChecklist.md).

## How this page is built

Every item below was **re-verified against the code during Phase 17**, not copied forward. Where the earlier register's description turned out to be incomplete or wrong, this page says so. Items numbered `F-*` were **found in Phase 17** and have no entry in the older registers; `F-7` onward came out of the transaction, concurrency, idempotency and tenant-isolation audits, and each was confirmed by reading the code rather than taken from the audit report.

Numbers are never reused. `F-5` is absent because it was closed, not forgotten.

**Severity**

| | Meaning |
|---|---|
| **P0** | Must be closed before any real customer or real money. Money loss, data loss, a broken deployment, or a legal bar |
| **P1** | Should be closed before the first commercial customer. Correctness or security weakness with a workaround, or an operational gap |
| **P2** | Acceptable to ship with a written limitation |
| **P3** | Future improvement; no release depends on it |

**Status**

`FIXED (Phase 17)` · `OPEN` · `BLOCKED — owner decision` · `BLOCKED — external verification` · `ACCEPTED — documented limitation`

**Evidence** is either *verified in code* (someone read the path and can name it), *reproduced* (a test or a run demonstrates it), or *inherited* (carried from an earlier pass and re-read, but the failure has not been reproduced).

---

## P0 — must be closed before real customers or real money

| # | Blocker | Evidence | Remediation | Tests | Status |
|---|---|---|---|---|---|
| R-02 | **A declined card could capture money against a cancelled order.** A `payment_intent.payment_failed` webhook cancelled the order while the intent stayed alive at `requires_payment_method`; the shopper retried on the same client secret, the money was captured, and the later success found a non-`Pending` order and returned an idempotent success. The `Payment` stayed `Failed`, which no refund path accepts | Verified in code and reproduced: the confirmation path asked the gateway for a boolean and treated every non-success as final, unlike the cancellation paths which already asked first | `PaymentConfirmationResult` now carries the intent's state; only a `Cancelled` intent cancels the order, a `Retryable` one leaves it `Pending` for the shopper to retry, and a capture against a closed order is recorded by `Payment.MarkCapturedAfterClose` so the normal refund path accepts it ([ADR-0036](../11-ADR/0036-payment-intent-state-machine.md)) | `PaymentTests`, `ConfirmOrderPaymentHandlerTests` (5 cases), `PaymentsAndRefundsTests` | **FIXED (Phase 17)** |
| R-13 | **Every uploaded image and store logo 404s through the shipped stack.** `/uploads/` was proxied with no `Host` override, so nginx sent `Host: api:8080`; `TenantResolutionMiddleware` runs on `/uploads`, resolves the store from the host, finds none, and answers `404 StoreNotFound` | Verified in code: the `/api/` location sets `proxy_set_header Host $http_host`, the `/uploads/` location set nothing | `frontend/nginx.conf` now forwards `Host`, `X-Forwarded-For` and `X-Forwarded-Proto` on `/uploads/` exactly as on `/api/` | *Needs a running stack to prove; no automated test covers the proxy* | **FIXED (Phase 17)** |
| R-17 | **A fresh production database is seeded with demo data.** `DbSeeder` applied the demo store's branding, three categories, eight products and their stock unconditionally — the environment was never consulted | Verified in code: `SeedCatalogAsync` and `ApplyDefaultStoreLookAsync` were gated only on "has no categories yet" and "settings never customised" | `DbSeeder.ShouldSeedDemoData` gates both on the environment, overridable in either direction by `Seed:DemoData`. Production initialization now keeps only what is configured deliberately (platform owner, first admin, host binding), and the seeder logs that demo data was skipped | `ConfigurationTests` (the decision table, including the explicit override in both directions) | **FIXED (Phase 17)** |
| R-06 | **The order confirmation email prints the wrong total, or never arrives.** `OrderEmailHandler` loaded the order through `GetByIdAsync`, which is `FindAsync` — the root alone, no lines. `Order.TotalAmount` then summed an empty collection, so the email showed shipping only; with a coupon, `Subtotal.Subtract(DiscountAmount)` went negative and `Money` threw, so the message retried until the outbox marked it dead | Verified in code: `OrderRepository` overrides only `GetWithItemsAsync`; nothing configures an auto-include | The handler loads the whole aggregate and reports the **frozen** figures (`PlacedSubtotal`, `PlacedTotal`) rather than recomputing at send time; the email is itemised, and the discount and shipping rows appear only when they exist | `OrderNotificationHandlersTests` (lines, a discount and shipping from the frozen columns; no discount or shipping row when there is none), `NotificationTests` (the delivered email carries the purchased line and the right total) | **FIXED (Phase 17)** |
| R-12 | **The application connects to SQL Server as `sa`.** A compromise or an injected query has full server privileges | Verified in `docker-compose.yml`: `User Id=sa` | Create a least-privilege SQL login owning only `SouqDb`, and change `ConnectionStrings__Default`. This is a deployment action, not a code change | *None — no test can assert the deployment's credentials* | **OPEN — deployment action** |
| R-16 | **No TLS, HSTS or security headers in the repository's own deployment**, and nginx overwrites `X-Forwarded-Proto` with its own (plain http) scheme, so links generated for emails can come out `http://` | Verified in code: nginx listens on port 80 only; the API calls neither HTTPS redirection nor HSTS | Terminate TLS in front, pass the outer scheme through instead of overwriting it, and add the header set before any public launch | `StartupAndSecurityTests` covers the API's own headers only | **OPEN — deployment action** |
| R-19 | **No automated backups and no rehearsed restore.** Data loss is unbounded, and `SECRETS_KEY` is unrecoverable — without it every stored store payment key is permanently unreadable | Verified: no backup job, schedule or script exists in the repository | Define and **rehearse** backup and restore of the database, the uploads volume and the secrets together, before the first paying store | *None* | **OPEN — operational** |
| R-01 | **JOD (and every three-decimal currency) may be charged at one tenth of the price.** `StripeAmountConverter` multiplies by 100 for everything Stripe does not list as zero-decimal. If the real connected account treats JOD as three-decimal, the correct multiplier is 1000 | Verified in code; the *behaviour of the real account* cannot be verified from this repository | Create a test intent for a three-decimal amount on the real account and compare the dashboard. Applies to BHD, IQD, KWD, LYD, OMR, TND as well — the original risk named only JOD | `StripeAmountConverterTests` pins the current arithmetic, not its correctness | **BLOCKED — external verification (P-05)** |
| R-25 | **No tax model exists.** The pricing pipeline's tax stage is a fixed zero | Verified in code: `PricingService` sets tax to zero explicitly | Decide inclusive/exclusive pricing, per-store rates and invoice requirements. A legal bar in most jurisdictions, not an engineering preference | `PricingServiceTests` asserts the explicit zero | **BLOCKED — owner decision (P-06)** |

## P1 — should be closed before the first commercial customer

| # | Blocker | Evidence | Remediation | Tests | Status |
|---|---|---|---|---|---|
| R-08 | **A suspended store answers 503 to everything**, including its own storefront configuration and sign-in, so it cannot show a branded "closed" page and its administrators cannot sign in to fix anything. `AvailableWhenStoreClosedAttribute` exists and is honoured — and was applied to **no endpoint** | Verified in code: a repository-wide search found the attribute only in its own definition, in `TenantAvailabilityMiddleware.IsOpen`, and in the documentation generator | The attribute is applied to exactly five endpoints — the storefront configuration, sign-in, refresh, sign-out and "who am I" — endpoint by endpoint rather than to a whole controller, so registration, password reset and every permission-protected endpoint stay closed. **Still open as a product decision:** how much administration a suspended store should retain beyond signing in (see P1 below) | `TenantResolutionTests` (catalog 503, config 200, admin signs in, admin endpoints still 503, registration 503) | **FIXED (Phase 17)** |
| R-07 | **A product in a disabled category is hidden but still purchasable.** `CatalogQueries.VisibleProducts` filtered on the category, but `PricingService` marked a line sellable from `Product.IsActive` alone, and checkout trusted that flag | Verified in code across the storefront, basket and checkout paths | Sellability is now one rule in the Domain — `Product.IsSellable` requires an active product **and** an active category — enforced by the pricing pipeline (so the basket and checkout share it), the basket add and the wishlist. `ProductRepository` loads the category with the aggregate | `ProductTests`, `PricingServiceTests`, `BasketHandlersTests`, `WishlistHandlersTests`, `CatalogTests` (a basket filled before the category is disabled, a fresh add, a direct checkout, and re-activation) | **FIXED (Phase 17)** |
| R-10 | **The public order tracking token is written to the request log.** `RequestLoggingMiddleware` logs `Request.Path`, and the token is a path segment of `/api/orders/track/{token}`; nginx's default access log records it too | Verified in code | Sensitive route parameters are redacted by **value** from the logged path, keeping the route template for aggregation. **Remaining gap (G-03):** the proxy's access log still records the SPA's token-bearing query strings for reset, verification and invitation links — a log-policy decision at the proxy | `ObservabilityTests` (the path is `/api/orders/track/***`, the template survives, the token is in no log entry) | **FIXED (Phase 17) — API side; proxy log policy still open** |
| R-22 | **`.env.example` shipped a non-empty Gmail placeholder**, which defeats the "no email provider" fail-fast: the API starts, selects Gmail, and every message dies after eight attempts | Verified in code: `AddEmail` selects Gmail on any non-empty `Gmail:AppPassword` | The placeholder is now empty, with the consequence written next to it | `NotificationTests` proves the fail-fast itself | **FIXED (Phase 17)** |
| TD-26 | **One transient failure of the payment-config request disabled card payment for the whole session.** `getStripePromise` cached `.catch(() => null)`, and `CardPaymentForm` reads `null` as "this store has no gateway" — so a momentary network error showed the shopper a "pay now" button with no card field. Pressing it confirms an order against an intent that never received a payment method, which since [ADR-0036](../11-ADR/0036-payment-intent-state-machine.md) answers a retryable `PaymentFailed` | Verified in code: one `catch` mapped every failure, transient or not, to "not configured", and the result was memoised for the lifetime of the page | The two answers are now distinguished: an empty key is a real, cacheable answer (no gateway — the direct-pay fallback is correct); a failed request is not cached and is re-asked, and the screen shows an error with a retry instead of silently degrading | `stripeClient.test.js` (a failure is re-asked, a real key loads Stripe once, an empty key is cached as "no gateway") | **FIXED (Phase 17)** |
| F-7 | **Disabling two administrators at once can leave a store — or the platform — with none.** `AccountStatusChanger.SetActiveAsync` counts active holders of the last-standing role, then disables *this* user and saves | **Found in Phase 17.** Verified in code: the count at `CountActiveByRoleAsync` and the write are separated, and the two requests write **different `User` rows**, so the rowversion on `User` never fires. A post-write re-count does not help either: under READ COMMITTED each transaction still sees the other's administrator as active | Needs real serialization — a stricter isolation level for this one operation, or a shared row both requests contend on. `IUnitOfWork` (a **Domain** interface) exposes no isolation level, and locking hints are raw SQL, which `TenancyRuleTests` forbids. So the fix is an architectural change, not a patch: **an owner/architecture decision**. Recovery today: a platform owner re-enables a store's administrator — but a locked-out **platform owner** has no in-product recovery at all | `AccountsTests` covers the sequential guard (`لا_يُوقف_آخر_مدير_فعّال`) with a stubbed count; the race is **NONE** | **OPEN — architecture decision** |
| F-8 | **A double-submitted checkout creates two orders**, double-reserving stock and spending two coupon uses from one basket | **Found in Phase 17.** Verified in code: `CreateOrderCommand` carries no idempotency key or client request id, `OrdersController.Create` carries no rate-limit policy (only Auth, Refresh, CouponPreview and Basket exist), and `CreateOrderHandler` has no precondition on an existing `Pending` order. The `BasketEmpty` guard does **not** cover this — the basket is emptied at payment, not at order creation, so a second checkout re-reads the same lines | No double charge (one client secret is rendered), and `ExpireStaleCheckoutsHandler` reclaims the stock and the coupon after `Inventory:ReservationMinutes`. Until then the shopper's *own* second click can fail with `InsufficientStock` on a low-stock item. **What a duplicate checkout should do — reject it, or return the order already created — is a commercial choice, so it is the owner's**: the engineering options are an idempotency key on the command, a pending-order precondition, or a rate-limit policy on the endpoint | **NONE** | **OPEN — owner decision** |
| F-9 | **The first two simultaneous orders in a new store could fail one of them with `409 DuplicateValue`.** `OrderNumbers` creates the store's counter row on first use and retried on a unique-index race — except the retry could never run | **Found in Phase 17.** Verified by type analysis: `AppDbContext.SaveChangesAsync` translates SQL 2601/2627 into `UniqueConstraintViolationException`, which derives from `Exception`, **not** `DbUpdateException` — so `catch (DbUpdateException)` was unreachable and the file's own comment described behaviour that never happened | The catch now names `UniqueConstraintViolationException`, with the reason recorded next to it so it is not "simplified" back | Covered indirectly by `OrderLifecycleTests` (`أرقام_الطلبات_متسلسلة_لكل_متجر_من_1001`, sequential); the concurrent first-order race itself is **NONE** — it is not deterministically reproducible without controlling transaction timing | **FIXED (Phase 17)** |
| F-10 | **Every deployment could duplicate an in-flight notification email.** `OutboxProcessor` sent the message, then recorded `ProcessedAt` with the dispatch token | **Found in Phase 17.** Verified in code: the success write passed `ct` while `RecordFailureAsync` deliberately passed `CancellationToken.None`. On shutdown between "email sent" and "row marked processed", the write was cancelled, the catch filter declined the `OperationCanceledException`, and the row stayed unprocessed and leased — re-sent once the lease expired | The success write is now uncancellable too, matching the failure path: the message has already left the system, so recording that is bookkeeping, not work to abandon | **NONE.** Not deterministically testable with the current harness: `CapturingEmailSender.Block()` gates on the dispatch token, so cancelling before release cancels the send itself and never reaches the write being fixed | **FIXED (Phase 17)** |
| F-16 | **An empty JSON body `{}` performed a real state change on eight endpoints, and answered 2xx.** `{}` is a *present* body, so the implicit body-required check passes, binding fills every field with `default`, and in each case member zero was the surprising outcome: ship an order, **activate a suspended store**, unblock a blocked customer, unpublish a product, disable a staff or platform account, force manual review moderation, and turn off every optional module | **Found in Phase 17** by the API-contract audit and confirmed in source. Each request type was a positional record over a non-nullable value type. `ChangeTenantStatusValidator` even carried `RuleFor(x => x.Action).IsInEnum()`, which **passes** for `0` — the guard gave false confidence rather than protection | Each required field is now nullable with `[Required]`, so absent is distinguishable from member zero and `[ApiController]`'s own validation answers `400 ValidationFailed` through the existing error contract. `TenantModulesRequest` keeps the distinction that matters: an absent field is refused, an explicit `[]` is still a legitimate "turn everything off". Verified empirically — the first attempt used `[property: Required]`, which MVC **throws** on for positional records, so the fix was proven wrong by test before it was proven right | `ErrorContractTests` (`{}` is refused on product status and review settings, and an explicit body still succeeds), `PlatformAdministrationTests` (`{}` no longer activates a store) | **FIXED (Phase 17)** |
| F-17 | **The storefront home page ranks by a full aggregate over the store's entire delivered order history, on every anonymous visit.** `CatalogQueries.BestSellingFirst` orders by a correlated `Sum` over delivered order items per product | **Found in Phase 17.** Verified in code: no index can serve this ranking, so the plan aggregates all delivered order items and sorts. `Store.jsx` requests `BestSelling` on every home-page load, and `/api/products/{id}/related` reuses the same ranking on every product page. Both are anonymous, uncached, and `/api/products` carries no rate-limit policy | Needs a measured fix, not a guess: a denormalised sales counter, a cached ranking, or a materialised "popular" list. Choosing between them needs numbers this repository cannot produce yet — there is no benchmark project and the largest dataset any test builds is 10 products | `CatalogTests` counts SQL commands for the catalog list, but only at the **default** sort with 10 products, so it would not catch this | **OPEN — needs measurement** |
| F-19 | **Timestamps read from the database serialize without a `Z`, so the SPA renders every stored time shifted by the viewer's UTC offset.** A value written by the clock keeps `Kind = Utc` and gets a `Z`; the same field read back from SQL Server has `Kind = Unspecified` and does not | **Found in Phase 17.** Verified in code: no DateTime value converter exists anywhere (every `HasConversion` is enum-to-int or the settings JSON), no DateTimeOffset is used in the Domain or Application, and no JSON date option is configured. The SPA parses with `new Date(iso)`, which treats an offset-less string as **local** time — so for a store in Amman, order dates, status history, review dates and notification times all display three hours early. One response can carry both forms at once | Emit UTC consistently — an EF value converter that stamps `Kind` on read, or offset-carrying types on the DTOs — and add a test asserting a serialized timestamp ends in `Z`. Worth doing **before** an external consumer compensates for it, after which correcting the value becomes a breaking change | **NONE.** No test anywhere asserts a serialized date string; `AuditTimestampsTests` asserts what is stored, not what is emitted | **OPEN** |
| R-03 | **Cancelling a paid order refunds it in full on `orders.manage` alone.** `TenantStaff` holds that permission and not `store.payments.manage`, so the daily operator can move money out | Verified in code: `UpdateOrderStatusHandler` calls the refund after the cancellation commits, with no payments-permission check | Either require the payments permission for a refunding transition, or audit order-status changes explicitly. Both change staff workflow, so the choice is the owner's | `UpdateOrderStatusHandlerTests`, `AuthorizationMatrixTests` | **OPEN — owner decision** |
| R-11 | **Rate limits are per process and partition on a forwarded client address.** With the API port published and the Docker bridge inside `TRUSTED_PROXY_NETWORKS`, a direct client may forge `X-Forwarded-For` | Inherited; depends on the deployment's networking and was not reproduced | Do not publish the API port when a proxy fronts the stack; trust only the proxy's own address; move limits to the edge when scaling out | `AuthSessionTests` covers the limiter, not the spoofing path | **OPEN — deployment action** |
| R-20 | **No health checks and no CI pipeline.** A broken deploy is found by a customer; a red suite can be committed | Verified: no health endpoint is mapped and no CI configuration exists | Add a health endpoint and a pipeline running the five suites. Requirements are written in [DeveloperQualityGates.md](DeveloperQualityGates.md) | — | **OPEN** |
| R-18 | **Migrations run automatically at startup in every environment**, so a bad migration is applied by the act of deploying, and two instances starting together race | Verified in code: `DbSeeder.SeedAsync` calls `MigrateAsync` | Back up first; move to a deliberate migration step before rollout | `MigrationRehearsalTests` covers the migrations, not the startup policy | **ACCEPTED for a single instance — documented** |
| R-26 | **The payment account model (D-13) is undecided**: every store connects its own Stripe account, or the platform adopts Stripe Connect. Until then the deployment account is the merchant of record by default | Verified: both mechanisms exist; the choice does not | Decide before onboarding a second paying store — it affects liability, fees and refunds | `PaymentsAndRefundsTests` covers both routing paths | **BLOCKED — owner decision (D-13)** |
| R-27 | **The repository is MIT-licensed with a public remote.** Anyone receiving the code may resell it | Verified: `LICENSE` is MIT | Decide the licence and repository visibility before the first sale | — | **BLOCKED — owner/legal decision (P-03)** |

## P2 — acceptable to ship with a written limitation

| # | Limitation | Evidence | Why it is acceptable today | Status |
|---|---|---|---|---|
| R-14 | Cross-module domain access is counted, not prevented (79 crossings) | Verified and **fully classified in Phase 17**: [ModuleBoundaryAudit.md](../02-ARCHITECTURE/ModuleBoundaryAudit.md) | Every crossing now has a written reason and a class; the ratchet stops new ones appearing quietly | **ACCEPTED — audited** |
| R-15 | Identity and Customers form a dependency cycle the target graph forbids | Verified: 12 crossings, 8 of them writes, in both directions | Neither module can be extracted alone, but nothing is incorrect at runtime | **ACCEPTED — documented** |
| R-04 | The store payment-account use cases live in the Platform feature folder although Payments owns the concept | Verified: they create and update the `StorePaymentAccount` aggregate from `Features/Stores` | The most sensitive code is attributed to the wrong module by the tests and docs; behaviour is correct | **ACCEPTED — documented** |
| R-09 | A store's currency locked only after a product or an order existed, so a coupon or shipping method created earlier kept the old currency | **Re-read in Phase 17 and found narrower than recorded.** Shipping was never exposed: `StoreShippingRates.QuoteAsync` filters methods to the basket's currency and `ShippingMethod.RateFor` throws on a mismatch, so a stale method is hidden, not mispriced. The coupon was exposed twice — `EnsureUsable` compared `MinOrderAmount.Amount` as a bare decimal, routing around the guard `Money.Add`/`Subtract` already enforce, and a fixed discount has no currency at all | Fixed at the source: `HasCommercialActivityAsync` now counts coupons and shipping methods, so the currency cannot change once either exists; `EnsureUsable` compares currency before amount, turning any pre-existing divergence into a loud refusal. **Residual:** `Coupon.Value` still has no currency column — unreachable now, and a schema change to fix (see [RiskRegister.md](../02-ARCHITECTURE/RiskRegister.md)) | `CouponTests` (a threshold in another currency is refused, not compared), `PlatformAdministrationTests` | **FIXED (Phase 17) — one structural residual recorded** |
| R-05 | A refund does not give a coupon use back | Verified; recorded in [ADR-0031](../11-ADR/0031-payments-and-refunds.md) | A deliberate product choice, not a defect | **ACCEPTED — product decision** |
| R-21 | Refresh tokens, stock reservations, in-app notifications, audit entries and dead outbox rows are never purged | Verified: only processed outbox rows are purged | Growth is slow at current volume; personal-data retention needs a legal decision first | **ACCEPTED — documented** |
| R-24 | Per-store sweeps run only for Active stores, so a suspended store's expired checkouts never settle and its baskets are never purged | Verified in `StoreSweepService`; the outbox dispatcher is deliberately status-agnostic, which is an inconsistency nothing recorded before Phase 17 | Stock stays reserved in a store that is not selling | **ACCEPTED — documented** |
| R-23 | In-process caches (tenant directory, session stamps) with no cross-instance invalidation | Verified | Correct for one instance, which is the only supported topology today | **ACCEPTED — single instance only** |
| F-1 | `ExecuteUpdate` / `ExecuteDelete` bypass the write guard, and **no architecture rule covers them**. `OrderNumbers.NextAsync` issues an unqualified bulk update whose isolation rests entirely on the query filter | **Found in Phase 17, re-confirmed by the isolation audit**, which enumerated all five call sites: `OrderNumbers`, `NotificationRepository.MarkAllReadAsync`, and three in `OutboxProcessor`. `TenancyRuleTests` scans for `IgnoreQueryFilters` and raw SQL but not for bulk operations | Not exploitable as written — EF applies query filters to bulk operations, and the outbox sites act on a deliberately unfiltered table from a platform-scoped service. The gap was that the **next** such call would have had no defence and no test. `TenancyRuleTests` now scans the Infrastructure IL for `ExecuteUpdate`/`ExecuteDelete` against a reviewed allowlist (`ReviewedBulkWrites`), exactly as it already does for `IgnoreQueryFilters`: the six existing call sites across three types are listed with their reason, and a new one fails the build until someone reviews it. These writes still skip `AuditTimestampsInterceptor`, so `UpdatedAt` is not stamped — recorded, not fixed | **FIXED (Phase 17)** |
| F-18 | `Page` has a lower bound and **no upper bound**. `PagedQueryValidator` enforces `Page >= 1` and `PageSize` in 1..100, so `?page=100000&pageSize=100` is accepted and becomes an OFFSET walk over discarded rows | **Found in Phase 17.** Verified in code; the `PagingRules` comment shows `pageSize` was considered as a denial-of-service lever and the offset was not. Anonymous on `/api/products`, which has no rate-limit policy, and it compounds with F-17 — each deep page also re-runs the aggregate ranking | Cheapest lever in the API, and a one-line bound to close. `QueryServiceTests` asserts 400 for `pageSize=101`, `page=0` and `pageSize=0`; **nothing asserts anything about high page numbers** | **OPEN** |
| F-20 | One condition returns two error codes: a duplicate caught by a handler's pre-check answers a specific code (`SlugTaken`, `DuplicateCode`, `EmailTaken`, …), while the same duplicate caught by the unique index under a race answers the generic `DuplicateValue` from `GlobalExceptionHandler` | **Found in Phase 17.** Affects all seven uniqueness conditions. A client branching on `code` handles the common path and falls through to a generic message exactly under concurrency, where the specific message matters most | Either map the constraint to its specific code, or drop the pre-checks and always answer `DuplicateValue`. Related: there is **no catalogue of the application error codes anywhere**, and nothing pins them — the 19 *domain* codes are pinned with a completeness guard, the rest are not | `ErrorContractTests` pins only the handler path | **OPEN** |
| F-21 | Five list endpoints are not paged, and two export endpoints are unbounded. `ApiDocumentation.md` states "every list endpoint is paged", which is false | **Found in Phase 17.** Categories and shipping methods return bare arrays with no server-side bound; `CustomerQueries.ExportAsync` returns every order a customer ever placed with every line in one joined result, plus all reviews, on an endpoint any signed-in customer can call with no rate limiter. Wishlist and addresses are bounded by domain caps and are fine | Bound the three genuinely unbounded reads, and correct the documentation claim | **NONE** | **OPEN** |
| F-3 | `Customer.UserId` and `RefreshToken.UserId` are single-column foreign keys, and the composite-FK architecture rule structurally cannot reach them because `User` is `ITenantOrPlatformOwned` | **Found in Phase 17.** Verified in the EF configurations and in the rule's own predicate | Unreachable today: every lookup is tenant-filtered and the guard stamps the tenant. The database, however, does not prevent it | **ACCEPTED — documented** |
| R-28 | The frontend stack decision (D-19) was deferred and its trigger had arrived | Verified, and the frontend was **measured** rather than recalled: 24 of 29 fetching components have no cancellation guard, 80 hand-rolled async-state declarations, 92 endpoint functions with no types | Decided in Phase 17 ([ADR-0037](../11-ADR/0037-frontend-server-state-and-types.md)): a query library is the target and its adoption point is named; TypeScript waits for a CI pipeline that can enforce it. No dependency was installed during a hardening phase | **DECIDED (Phase 17)** |

## P3 — future improvement

| # | Item | Note |
|---|---|---|
| F-2 | On a platform host, `/uploads/tenants/{anyId}/…` serves any store's files: the middleware returns before the ownership check | **Found in Phase 17.** Deliberate per [ADR-0022](../11-ADR/0022-tenancy-enforcement.md); filenames are 128-bit random |
| F-4 | The unique index on reviews is `(CustomerId, ProductId)` — the only unique index on tenant-owned data that does not lead with `TenantId` | Harmless while ids are global; becomes a cross-store conflict oracle if ids ever become per-tenant |
| F-6 | A domain serves the storefront the moment it is added; `VerifiedAt` is recorded but never gates anything | Platform-only action, and a host cannot be stolen from another store |
| R-05a | Every refund is sent to the gateway with the reason `requested_by_customer`, whatever the real reason was | Cosmetic in the provider dashboard |
| F-11 | Two late webhooks arriving together for a captured-but-cancelled order both call `MarkCapturedAfterClose`; the loser's `ConcurrencyConflictException` is uncaught and becomes a 500 | **Found in Phase 17.** Self-healing: the gateway retries, the third pass sees `Succeeded` and records nothing. Noisy, not wrong. The sequential case is covered by `ConfirmOrderPaymentHandlerTests` |
| F-12 | Check-then-insert races on wishlist add, review create and basket lines surface as `409 DuplicateValue` on actions a user reads as idempotent (clicking a heart twice) | **Found in Phase 17.** The unique indexes keep the data correct; only the message is wrong. A UX fix, not a correctness one |
| F-13 | A file is written to `IFileStorage` before `SaveChangesAsync`, so a failed save leaves an orphan file | **Found in Phase 17.** Storage leak only — the row never references it. Cleaning orphans is already **FUTURE** in the catalog rules |
| F-14 | **Forward hazard, not a defect today:** the SQL Server provider is registered without a retrying execution strategy. Turning *EnableRetryOnFailure* on later would throw at every explicit `BeginTransactionAsync` unless each of the seven transactional use cases is wrapped in the strategy | **Found in Phase 17.** Deliberately deferred to Phase 23 by [ADR-0021](../11-ADR/0021-transaction-boundaries.md); recorded here because the change breaks them all at once rather than one at a time |
| F-15 | Two `DependencyRuleTests` rules selected entities by matching the namespace string `Souq.Domain.Entities`, so the four entities outside it — `User` and `RefreshToken` in `Souq.Domain.Identity`, `Tenant` and `TenantDomain` in `Souq.Domain.Platform` — were covered by neither. Accounts, sessions and stores could have grown a public setter, or been bound directly by a controller, without failing the build | **Found in Phase 17.** Latent, never violated: there is no public setter anywhere in the Domain (226 are `private set`) and no controller action takes a domain entity. Both rules now select by `IsSubclassOf(Entity)`, the predicate `TenancyRuleTests` already used, taking coverage from 25 entity declarations to 29 while still excluding the Platform *settings* classes, which are not entities. **FIXED (Phase 17)** |

## Not yet assessed

These are named in the mission and not yet re-verified in code; they carry no severity until they are.

- Performance: N+1 queries and unbounded reads on the admin and reporting paths.

**Assessed during Phase 17 and no longer on this list:**

- **Transaction boundaries and concurrency.** No use case splits a logical write across transactions that should be one, and **no transaction spans a network call** — the rule ADR-0021 states is genuinely kept: `CreateOrderHandler` creates the payment intent after its commit, `UpdateOrderStatusCommand` refunds after its commit, and `OrderPaymentConfirmation` confirms before opening one. Every contended aggregate carries a rowversion (Product, Coupon, Order, Payment, InventoryItem, Tenant, User); the four that do not — `CouponRedemption`, `Refund`, `StockReservation`, `Basket` — are covered by their aggregate root's token or, for the basket, deliberately last-writer-wins with checkout re-validating. The open items are F-7 and F-8.
- **Idempotency.** Duplicate Stripe webhooks, duplicate `confirm-payment`, refund retries and coupon reserve/confirm/release are all protected, and the mechanism is worth knowing: the webhook handler **discards the event's payload and type** and re-asks the gateway for the intent's live state, which is what makes both redelivery and out-of-order delivery safe without a processed-events table. Order creation (F-8) is the one unprotected entry point.
- **Tenant isolation.** Audited across all 13 modules against the controllers rather than the generated inventory: no cross-tenant or cross-customer path was found. Every mechanism ADR-0022 claims was confirmed present and applied by reflection across the model, and `TenantIsolationTests` enumerates the live endpoint list so the matrix cannot silently drift. The residual items are F-1 and F-2, both already recorded.

## How to use this page

1. **Before a release:** everything in P0 is either `FIXED` or has a deliberate, written acceptance from the owner. No exceptions — P0 is defined as "money, data, deployment or law".
2. **Before the first commercial customer:** work down P1. Items marked *owner decision* cannot be closed by engineering alone; take them to the owner as a batch.
3. **When an item closes:** move it to `FIXED` with the commit, and delete its row from [RiskRegister.md](../02-ARCHITECTURE/RiskRegister.md) and its entry in [TechnicalDebt.md](../12-ROADMAP/TechnicalDebt.md) in the same change. This page keeps the history; those two keep only what is still true.
