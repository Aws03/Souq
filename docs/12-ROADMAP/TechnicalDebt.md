# Technical debt register

> **What this page is:** work that is known, deliberate and unfinished. Every entry was found by reading the code during the engineering-knowledge pass, not invented to look thorough. Nothing here is a defect report — defects and dangers live in [RiskRegister.md](../02-ARCHITECTURE/RiskRegister.md); several entries below are the *fix* for a risk, and say so.
> **How to use it:** pick by priority, read the linked module document first, then follow [HowToChangeExistingCode.md](../00-START-HERE/HowToChangeExistingCode.md). When you close one, delete the row in the same commit.

**Priority** is "how much does it cost the next team": **P1** blocks or misleads day-to-day work · **P2** slows work or hides risk · **P3** tidy-up worth doing when nearby.

## Structure and boundaries

| # | Debt | Why it exists | Impact | Fix | Priority | Prerequisites | ADR? |
|---|---|---|---|---|---|---|---|
| TD-01 | **Module boundaries are enforced only inside `Souq.Application.Features`.** Repository ports and entities live in shared Domain namespaces, so 74 cross-module crossings compile silently | The Domain was organized by kind before modules existed (ADR-0002's per-layer namespaces were never applied outside Application) | A new engineer reads "enforced" and adds the 75th in good faith; extraction gets harder | The ratchet now counts them ([ModuleDomainDependencies.md](../02-ARCHITECTURE/ModuleDomainDependencies.md)). Next: agree the target graph, then extract contracts module by module | P1 | Agreement on the target graph | Yes, if ownership moves |
| TD-02 | **Three contracts are named in the docs but do not exist:** *ICustomerDirectory*, *IUserDirectory* / *IAccountTokens*, *IOrderHistory*. *ISellableItems* remains named too, but re-ranked (M1): [ModuleBoundaryAudit.md](../02-ARCHITECTURE/ModuleBoundaryAudit.md)'s rank table, not this row, is the authority on which to extract first — the class-D writes (the Identity⇄Customers cycle, *IAccountTokens*) close more real risk per file than the read-only *ISellableItems* cluster does, even though Shopping → Catalog has the most crossings | Each was deferred when its phase shipped; the docs kept the name. The original "start with Shopping → Catalog, it hurts most" claim in this row undersold the class-D writes and was corrected in the M1 architecture audit | Three module pairs reach into domain repositories instead of a contract | Extract in rank order from [ModuleBoundaryAudit.md](../02-ARCHITECTURE/ModuleBoundaryAudit.md): the Identity⇄Customers cycle first (also TD-03), then *IAccountTokens*, then the read-only clusters as their owning module is next touched (*ISellableItems* with Catalog/M2, *ICustomerDirectory*/*IOrderHistory* with Shopping-Ordering/M5) | P1 | TD-01's target graph | No |
| ~~TD-03~~ | ~~**Identity and Customers form a cycle**~~ **Closed in M9: the cycle is gone.** The two contracts the M1 audit named now exist — `IAccountProfiles` (declared by Identity, implemented by Customers: create a profile for a new account, find the profile for an account) and `IAccountLifecycle` (declared and implemented by Identity: verify a password, rename, erase). Both are declared by Identity **on purpose**: a contract pointing the other way would have documented the cycle rather than broken it, because the cycle is two arrows and only one may survive. Now every reference runs Customers → Identity and nothing comes back. **Measured, not asserted:** the generated crossing ratchet went from **74 crossings across 15 pairs to 62 across 13** — all twelve Identity↔Customers crossings are gone and both pairs left the table. Identity no longer names `Customer` or `ICustomerRepository`; Customers no longer loads `IUserRepository`, mutates `User`, or revokes `RefreshToken` rows, so session invalidation is back inside the module that owns sessions. Test coverage moved with the responsibility rather than shrinking: what the erase does to the account (stripped, disabled, tokens revoked, stamp forgotten **after** the save) is now `AccountLifecycleTests`, and building the customer aggregate is now `CustomerAccountProfilesTests` — both were previously asserted by the other module's tests, which is the same boundary violation in test form | Registration and erasure were built on both sides at different phases | ~~Neither module can be tested or extracted alone~~ — each can now | Done | ~~P1~~ Closed | None | Yes — done |
| TD-05 | **`Souq.Domain` has no module namespaces**, unlike the Application layer | Never done; ADR-0002 assumed it | Ownership of an entity is only discoverable through the `DomainOwners` map in the tests | Either move types into per-module namespaces, or record that the map is the source of truth (cheaper, chosen for now). **Re-confirmed in the M1 architecture audit:** the map stayed accurate and comprehensive through the payment-account contract extraction (TD-04) with no drift, `GeneratedDocsTests` and `DocumentationTests` both still pass, and a namespace move would have widened M1's blast radius for no defect it fixes — the cheaper choice still holds on today's evidence | P3 | TD-01 | Yes if moved |

## Correctness and consistency

| # | Debt | Why it exists | Impact | Fix | Priority | Prerequisites | ADR? |
|---|---|---|---|---|---|---|---|
| ~~TD-06~~ | ~~**Two coupon evaluations.**~~ **Closed in M8: the endpoint was deleted.** `GET /api/coupons/apply` took the subtotal from the caller, so it skipped the per-customer limit and measured the coupon's minimum against a number the caller chose — and, with no validator and no upper bound in `Money` beyond the currency's minor units, it answered `subtotal=999999999` with a ~150,000,000 discount as a `200`. **One clause of this item was wrong and is corrected for the record:** it did *not* skip the module check. `RequiresModule(Promotions)` sits on the controller and `TenantAvailability` runs before authentication, which `PlatformAdministrationTests` asserted against this very route. Of TD-06's two proposed fixes, "reimplement it over `IPricing`" collapses into the other endpoint — pricing the caller's real basket through `IPricing` is precisely what `GET /api/basket/quote` already does, anonymously via the guest basket and under the same `coupon-preview` limit — so deletion lost no capability and BR-CPN-09 now names the basket quote. No frontend code called it (`client.js` had no entry). Three integration tests used it only as a convenient anonymous specimen for cross-cutting rules — the problem+json contract, module gating and tenant isolation — and were **re-pointed rather than deleted**, so all three rules still hold. Its two unit tests were removed because both rules they covered live in Domain and are tested there (`CouponTests`, `MoneyTests`) | Predates the pricing pipeline; the SPA stopped using it | ~~A publicly reachable endpoint can accept a coupon that checkout then rejects~~ — no longer reachable; one evaluator remains | Done | ~~P1~~ Closed | None | No |
| TD-10 | **One failure model, two conventions.** The same business condition is a `Result` in one module and an exception in another; three places throw raw `InvalidOperationException` for a missing tenant (500) | Conventions settled after some code was written | The HTTP status of a condition depends on which path reached it | State the rule (invariants throw, use-case outcomes return `Result`), then align the outliers | P2 | None | No |
| TD-11 | **Thirty commands have no validator**, including every upload command | Validation was added per phase, where it was felt necessary | A reader cannot tell "nothing to validate" from "validated elsewhere" | Document the rule; add validators where input is user-supplied | P2 | TD-10 | No |
| TD-12 | **Store-side actions are not audited:** order status changes, coupons, shipping methods (platform actions are) | The audit behaviour was built for the platform area | Coupon and shipping-method changes leave no record of who made them. Order status changes are recorded in the order's own history with the staff member's name (`OrderLifecycleTests`), but not in the audit log, so they can't be searched across orders or seen by the platform | Decide the store-side audit scope, then mark those commands auditable | P2 | Product decision | No |
| TD-14 | **Three hand-rolled retry loops** (inventory, coupons, payments), each with its own `MaxAttempts` and reset convention | Written per phase | Divergent behaviour under conflict; three places to fix a bug | One documented retry helper | P3 | None | No |
| TD-15 | **`IRepository<T>` gives all sixteen ports a `Remove` the model forbids** for most aggregates (one implementation throws to say so) | A generic base from before the aggregates diverged | Invites deletions the domain does not allow | Drop `Remove` from the base; add it to the six ports that mean it | P3 | None | No |

## Data and operations

| # | Debt | Why it exists | Impact | Fix | Priority | Prerequisites | ADR? |
|---|---|---|---|---|---|---|---|
| TD-16 | **No retention anywhere:** refresh tokens, stock reservations, in-app notifications, audit entries and dead outbox rows are never purged | Only the outbox purge was in scope (Phase 14) | Unbounded growth; personal data kept indefinitely (risk R-21) | Decide retention per table, then extend the existing sweep services. **Re-filed in M9 with the split made explicit, because only part of it needs the owner.** Refresh tokens are the one table with no legal question attached: they hold no personal data beyond a user id, and their own `ExpiresAt` already says when a row stops meaning anything, so a purge of expired-or-revoked tokens older than the refresh lifetime is a purely technical change and the natural first slice. Stock reservations are similar. Audit entries and in-app notifications are the ones that genuinely wait: retention there is a legal and product question about how long a store keeps a record of who did what, and M9 did not answer it | P2 | Legal decision on audit and notification retention — **not** on refresh tokens or reservations | No |
| TD-18 | ~~**No design-time `DbContext` factory**~~ **Closed in M17.** `DesignTimeDbContextFactory` reads only `ConnectionStrings__Migrations` (else `__Default`, else a model-only local string) from the environment, so `dotnet ef migrations list --project src/Souq.Infrastructure` runs from a clean checkout with no API host, no appsettings and no secrets — verified. It stopped being a convenience the moment R-18's deliberate migration step became an option: that step runs from a deploy machine or a bundle, and neither has — nor should have — the runtime's secrets | Never needed locally | — | Done | Closed | None | No |
| TD-19 | **`ExecuteUpdate`/`ExecuteDelete` bypass the interceptors** (no audit timestamps, no write guard) at six call sites in three types (`OrderNumbers`, `NotificationRepository`, `OutboxProcessor`); for the two tenant-owned tables only the query filter keeps them tenant-safe | Chosen for set-based performance | A new call site no longer slips in: `TenancyRuleTests` fails until it is added to `ReviewedBulkWrites` with a reason. What remains is that these writes never stamp `UpdatedAt`, and their tenant safety rests on the filter rather than the guard | Stamp the timestamp explicitly at each site, or a guarded helper | P3 (was P2) | None | No |
| TD-20 | **Uploads live on local disk** | Local storage was the Phase 1 choice; blob storage is Phase 23 | Blocks a second instance without a shared mount (risks R-13, R-23) | Move to blob storage behind `IFileStorage` before scaling out | P2 | Phase 23 | No |
| TD-21 | **Configuration read outside the options convention** in two places (`FRONTEND_URL`), and one options class bound without validation | Added late in two different layers | The convention says every section is validated; two are not | One validated options type | P3 | None | No |
| TD-22 | **The Domain knows the storage URL layout** (branding asset paths must start with the local storage prefix) | The validation was added with local storage | Swapping to blob storage would make the Domain reject every asset URL | Pass the expected prefix in, or validate the shape only | P2 | Before blob storage | No |

## Frontend

| # | Debt | Why it exists | Impact | Fix | Priority | Prerequisites | ADR? |
|---|---|---|---|---|---|---|---|
| TD-24 | **Feature folders are half-migrated:** pure logic lives in `features/`, screens in `pages/`, and `api/client.js` is one module with about a hundred endpoints | The migration was deferred to the phases that rebuilt those screens; Phases 16 and 17 closed without doing it, deliberately (roadmap Phase 16) | "Where does this go?" has two answers | Move each screen when it is next substantially changed. **M10 did this where it applied and is not claiming more:** the nine admin screens it touched had their pure logic already in `features/` (`orderView`, `customerActions`, `couponForm`, `reviewModeration`, `shippingForm`, `synonymForm`, `productPayload`), so there was nothing to move for them; what M10 *added* went to `features/` by default (`statusTone.js`, `passwordForm.js` in M9). `api/client.js` is untouched and remains the substantive half of this item | P2 | None | No |
| ~~TD-25~~ | ~~**The admin screens still fetch by hand.**~~ **Closed in M10: all of them are on the query layer.** Inventory moved in M7; Categories, Coupons, Customers, Orders, Products, ReviewModeration, SearchSynonyms and ShippingMethods moved in M10. The lint rule this item cites, `react-hooks/set-state-in-effect`, went **20 hits → 7**, and none of the seven is an admin screen — they are storefront and checkout components outside this item's scope, listed here for whoever takes them: `Catalog`, `NotificationBell`, `OpeningExperience`, `WishlistContext`, `useStoreSearch`, `ProductDetail`, `CardPaymentForm`. **The defect was demonstrated before it was fixed.** Pagination is the narrow case; search is where it bites — typing a word fired a request per keystroke, so an earlier reply could paint over a later one, leaving a table of results for something other than what was written in the box. `Orders.test.jsx` (the screen had no test at all) was run against the pre-migration screen and observed to fail on both counts. Search is now debounced, matching `platform/Stores.jsx`, and the page reset moved from an effect into the handler, which is what takes the count to zero. **Payments was left deliberately:** it has no lint hit (its setState is inside `.then`, not the effect body) and no reachable race — it fetches on mount and after save, with no criteria to outrun — so migrating it would mean extracting a form component to seed state from query data, a structural change to the payments screen with no defect behind it | Written before the pattern settled; Phase 17 rebuilt the admin area without migrating these screens | ~~Stale data on screen in the admin area~~ — no longer reachable on any admin screen | Done | ~~P3~~ Closed | None | No |
| ~~TD-28~~ | ~~**Five status→CSS-class maps**~~ **Closed in M10 — and it was not five.** An inventory found **nine named maps, eight inline ternaries and six implicit `status.toLowerCase()` lookups** across four CSS modules: three byte-identical copies of the same five rules, and a fourth with the same five names at two different values, because a WCAG contrast fix had been applied to only one of them. Twenty-two classes collapsing to five real tones. `features/statusTone.js` is now the only place a status becomes a tone and `StatusBadge` the only place a tone becomes pixels. **Four visible contradictions fixed:** `Succeeded` was blue for a payment and green for a refund *in the same drawer*; "enabled" was blue on two tables and green on two others; `invited` was amber on one screen and blue on another **from the same helper**; and order `Pending` rendered at two contrast grades. Each self-consistent status kept its tone — changing those is a redesign, not a unification. **The amber tone had no token at all** (two hex literals), so it never derived for dark mode: `--color-warning`/`--color-warning-soft` now join the status tokens and derive per theme with guaranteed contrast, verified live in both modes | Copied per screen | ~~Inconsistent colours; a rename restyles unrelated screens~~ | Done | ~~P3~~ Closed | None | No |
| ~~TD-29~~ | ~~**A password-change screen does not exist**~~ **Closed in M9: built at `/account/password`.** A route rather than a fourth panel on `/account`, because Phase 16 split that page precisely for carrying three concerns. It joins `AccountLayout`'s navigation. The screen's promise matches the handler: every other session is revoked and a fresh one issued for this device, so "signs you out everywhere else" is a description, printed above the button rather than after it. A wrong current password attaches to its own field, because the server returns a 400 with a stable code rather than a 401 that the client would read as an expired session and act on. The rules also stopped being copied: `Register` and `ResetPassword` each had their own length-only check, so a password without a digit passed the browser and was refused by the server; all three screens now share `features/account/passwordForm.js`, which mirrors `PasswordRules` including letters-and-digits | The screen was never built | ~~A signed-in customer cannot change their password~~ — they can | Done | ~~P2~~ Closed | None | No |
| TD-42 | **A store cannot publish content pages** — no privacy policy, terms, returns, shipping or FAQ. The footer used to link to all six with `href="#"`; Phase 16 removed the dead links rather than keep the appearance | No content capability was ever built; the footer was written as a visual placeholder and never revisited | A store selling in most jurisdictions needs published terms and a privacy policy. Today the platform cannot give it one, and the storefront no longer pretends otherwise | A small content capability: store-authored pages with a slug and per-language body, rendered at `/pages/:slug`, linked from the footer when they exist — **or** a much smaller one, policy-link fields on `StoreSettings`. Both options, the trade-off and a recommendation are now fully written up: [OwnerDecisions.md](../09-OPERATIONS/OwnerDecisions.md#td-42--store-authored-legalinformational-pages-how-much-of-a-capability) | P2 | **Confirmed still blocking (M2's own attempt to build it hit exactly this fork):** a product decision on scope (authored pages vs. a link field per policy) | Yes, if (a) is chosen |

| TD-53 | **The account lockout guards sign-in but not the password change.** Five failed sign-ins lock an account for fifteen minutes (`User.RecordFailedLogin`), and that counter is touched only by the login handler. `POST /api/auth/change-password` verifies the current password with no attempt counter at all, so the only brake is the shared `auth` rate limit — 10 requests per minute per **(host, IP)**, a bucket it shares with login, register and the reset flow | The lockout was built for the sign-in path, and the change-password endpoint was read as already-authenticated and therefore safe | Narrow but real: reaching it needs a valid access token, so the attacker is already inside a session — what it buys them is *persistence*, changing the password to lock the owner out. Rotating IPs would widen the 10/min brake. Found in M9's audit; measured, not hypothetical | **This is a trade-off, not an oversight to correct blindly:** extending the lockout here means a customer who mistypes their current password five times loses access to their whole account for fifteen minutes, which turns a fumbled form into a sign-in failure. The alternatives are a separate, looser counter for this endpoint, or a per-account (not per-IP) rate limit. Pick one deliberately — M9 filed it rather than choosing, because it changes what locks a real customer out | P3 | None — but the choice above is a product call as much as a security one | No |
| TD-54 | **Three status tones are defensible-but-unexamined, and one badge is not a status at all.** M10 unified the tone vocabulary and fixed every case where two screens disagreed, but deliberately did not re-decide a tone that is self-consistent — that is a redesign, not a unification. The four left, each with the argument against it already written at its line in `features/statusTone.js`: product `Draft` is a warning though it is not a problem, just unpublished (neutral is arguably right, and neutral only became available in M10); coupon redemption `Released` is a danger though release is the *normal* end of an abandoned basket; payment `Cancelled` shares danger with `Failed` though one is intentional and the other is not; and the Stripe key mode badge (`live`/`test`) is an **environment flag**, not a domain status, borrowing success/warning because nothing else existed | Each was chosen per screen before there was a shared vocabulary to choose from | None — every one of them is legible and self-consistent today. This is about meaning, not contrast | Decide each as a design question, now that they are four lines in one file rather than twenty-two classes in four modules. The key-mode badge probably wants to leave the status vocabulary entirely | P3 | A design decision, not a technical one | No |
| TD-45 | **Matching a word *inside* a name is still a narrow-index scan, not a seek.** M3 made exact and prefix matches index seeks on `IX_ProductTranslations_TenantId_NameNormalized`, but `LIKE '%word%'` (the case where the query word sits in the middle of a product name) cannot seek | A leading wildcard makes any B-tree index unusable by construction. M3 chose the narrow covering index — a scan of two short columns instead of a table holding a 4000-character description — over building an inverted-term table, because the measurement said it was not needed yet ([ADR-0042](../11-ADR/0042-local-search-engine.md), "Measured evidence") | None at measured sizes: 0.2 ms for the seek path, and the scan replaced a 14.4 ms table scan. It grows linearly with one store's catalog, so a store far larger than any measured here would feel it | An inverted-term table: one row per (store, culture, product, word), indexed on (store, culture, word), turning word matching into a seek. The design is written up in ADR-0042 "Revisit when". It is explicitly the step to take **before** any external search service | P3 | A measured latency breach on a real catalog — not a guess about scale | No (ADR-0042 already records the decision and its trigger) |
| TD-46 | **No linguistic stemming.** Arabic morphological variants do not match each other unless a synonym or correction row says so: a query for one inflection will not find a name written with another | Arabic root extraction is a substantial piece of language engineering, and M3 deliberately shipped folding (which is exact and testable) rather than guessing at morphology. SQL Server Full-Text Search would not have helped: it is measured unavailable in the pinned image ([ADR-0042](../11-ADR/0042-local-search-engine.md)) | Recall gap on inflected queries. Unknown in size until M13 logs real no-result searches — which is exactly what M13 exists to measure | Either a local Arabic light-stemmer applied to indexed text and queries alike (same function on both sides, as the normalizer is), or letting merchant-editable synonyms cover the cases that actually occur. Decide from M13's data, not from principle | P3 | M13 (search analytics) showing a recurring pattern that folding and edit distance cannot recover | Yes, if a stemmer is added — it changes the stored index and needs a reindex |
| TD-47 | **The storefront leaves a ~2 px horizontal overflow at 320 px while the mobile menu sheet is open.** Measured on the container stack during M3's browser pass: 0 px with the sheet closed, ~2 px with it open. The elements sitting outside the viewport are the announcement marquee track and the horizontally-scrolling discovery rows and category nav — all pre-existing, all inside scroll containers, so most are false positives; the residual page-level 2 px is not | The announcement bar is an animated marquee whose track is deliberately wider than the viewport, and opening the sheet locks `body` scrolling (`useDialog`), which changes how the page measures | Cosmetic at this size — a 2 px rubber-band on one screen state, not a clipped control. It was **not** introduced by M3: the defect M3 did introduce at that width (a 129 px overflow from the fourth sort button) was found by the same pass and fixed in the same change | Identify whether the residual 2 px is the marquee track or the locked-scroll measurement, and fix at the source. M4 owns the systematic pass over every screen at phone width, so it belongs there rather than as a one-off patch here | P3 | None — M4 schedules the work | No |
| TD-48 | **Dialogs have no cyclic focus trap.** `useDialog` moves focus in, restores it on close, closes on Escape (topmost only) and locks background scrolling — but Tab can still walk out of an open dialog into the page behind it. `ProductZoom` hand-rolls the same four behaviours and has the same gap | A real trap needs focusable-element enumeration and re-entry handling; Phase 16 implemented the four behaviours that make a dialog *usable* by keyboard and stopped short of the one that makes it *confining*, deliberately, rather than shipping a partial guard | A keyboard or screen-reader user can Tab into the page behind an open drawer and interact with controls that are visually obscured. Escape still closes, so nobody is stuck | Enumerate focusable descendants on open and cycle Tab/Shift+Tab at the boundaries, in `useDialog`, so every dialog gains it at once — and make `ProductZoom` use the hook rather than its own copy | P2 | None | No |
| TD-49 | **`NotificationBell` declares `role="dialog"` for a non-modal popover.** It closes on Escape and outside click but does not move focus or lock scrolling, which is right for a popover and wrong for the role it claims | The role was chosen for the panel shape rather than its behaviour | No measured failure: axe is clean across every storefront route (M4). But a screen reader announces a dialog and then finds none of a dialog's behaviour | Either drop to a non-modal disclosure pattern (button with `aria-expanded` controlling a plain region) or adopt `useDialog` | P3 | None | No |
| TD-50 | **A payment records the *kind* of Stripe account that took it, never *which* one.** `Payment.Gateway` holds `stripe:store`, so a refund resolves the store's **current** account. Replace a store's keys between charge and refund — including the ordinary test→live switch the entity explicitly supports — and the refund goes to an account where that intent does not exist: Stripe answers 404/400, the refund is marked `Failed`, and a failed refund cannot be retried. The customer's money then has no in-app path back | The field was added to route between the deployment account and a store account, which only needs the kind; account *replacement* was not considered | Reachable through ordinary onboarding, not an exotic scenario: connect test keys → take a test payment → switch to live keys → that payment can never be refunded in the app. Three places in the repository claimed the opposite until M6 corrected them (`Payment.cs`, `PaymentGatewayRouter.cs`, `Security.md`) | Record the account identity alongside the kind (the publishable key, or a hash of it) when the intent is created, and refuse — with a clear operator message — to refund through an account that does not match, instead of sending it and failing. That is a payment behaviour change, so it needs an ADR and tests (AGENTS.md §0 rule 3); M6 deliberately corrected the false claims and filed this rather than changing money behaviour inside an audit phase | P1 | An ADR; ideally decided together with D-13, since Stripe Connect would change what identity even means | Yes |
| TD-51 | **A refund retry is only idempotent for 24 hours.** The key `souq-refund-{tenantId}-{refundId}` is stable across retries, but Stripe forgets idempotency keys after 24 hours. `RetryRefundAsync` gates only on `RefundStatus.Pending`, with no age limit and no reconcile against the provider, so retrying a refund whose original call was never answered, a day or more later, is a fresh request — and if that original call had actually succeeded, the customer is refunded twice | The retry was designed against the key's guarantee without recording that the guarantee is time-bounded; three places stated it unconditionally until M6 measured it | Narrow: it needs a call the gateway never answered **and** an operator pressing retry more than a day later. But the dashboard actively nudges toward that button, and the outcome is money leaving the store twice with nothing recording that it could | Either refuse to retry a `Pending` refund older than the provider window and require an explicit reconcile, or ask the provider for the refund's real state before retrying. Both change payment behaviour, so both need an ADR (AGENTS.md §0 rule 3) — M6 documented the bound rather than changing money behaviour inside an audit phase | P2 | An ADR | Yes |
| TD-52 | **No test exercises `PaymentGatewayRouter`, and the `stripe:store` branch cannot be tested offline.** The router constructs a `StripeGateway` inline, so exercising the store-account path would talk to Stripe over the network even in Testing. Every routing rule D-13 depends on — store account when connected, deployment otherwise, the account kind recorded on the payment for later calls, `503` rather than a silent fallback when a secret cannot be decrypted — currently rests on reading the code | The router was written when only the deployment path existed; the store-account branch arrived in Phase 11 without a seam to fake the gateway | M6 verified all four rules by reading the code and found them correct, so this is a **coverage** gap rather than a half-built path. But it is the one place where a D-13 answer will land, and it would land without a safety net | Extract the two-line gateway construction behind an injectable factory (a `Func<string, StripeCredentials, IPaymentGateway>` registered in Infrastructure DI, defaulting to today's `new StripeGateway(...)`), then test the routing rules against a fake. Behaviour-preserving by construction; it is a testability change, not a payment-behaviour change | P2 | None — but best done together with whatever answers D-13, so the new path gets tests from the start | No |
## Tests and tooling

| # | Debt | Why it exists | Impact | Fix | Priority | Prerequisites | ADR? |
|---|---|---|---|---|---|---|---|
| TD-31 | **CI does not block a merge — and since before M13 it does not run at all.** [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) is comprehensive and M18 fixed the three defects that had been making it red (a platform-dependent regex in a generated doc, two `-warnaserror` warnings, and a flaky metrics test). But every run since `35313506879` failed in ~3s with *"the job was not started because recent account payments have failed"* — the repository is private, so Actions minutes are billed | Branch protection is a GitHub setting no file can contain, **and billing is an account matter no file can contain either** | A red run can still be merged, and no run reports at all; every "CI runs X" claim in the docs describes configuration, not observed execution | Two owner actions, both in [OwnerDecisions.md](../09-OPERATIONS/OwnerDecisions.md): resolve billing (or make the repository public), **then** switch on branch protection. Meanwhile `scripts/ci-local.sh` runs the fast job on Linux locally — it found four real defects in M18 | P1 | A repository setting and an account setting, both owner-only | No |
| TD-32 | **Frontend component tests: the forms are now covered; most admin drawers are not.** **Closed in M19:** the three forms this row named — address (`AddressFormDrawer.test.jsx`), profile (`ProfileForm.test.jsx`) and review (`ReviewForm.test.jsx`) — each asserting what only a screen can: what reaches the server versus what is in form state, which field an error attaches to, and whether a rejection loses what was typed. Writing them found a real accessibility defect in `StarRating`, which both share | The environment was deferred through Phases 1–15; it arrived with the storefront rebuild | **What remains, counted in M19:** 27 admin pages and drawers under `frontend/src/pages/admin/` have no component test of their own — the product, category, coupon, shipping, synonym, staff-invite, stock and order drawers above all. They are exercised by the Playwright journeys, which is real coverage but slow, serial, and unavailable in CI | Cover the drawers that carry money or destructive confirmations first (order status and refunds, stock, coupons); the rest are lower value while the journeys run | P3 | — | No |
| TD-33 | **The Stripe adapter and the email providers have no tests**; only the fakes are covered | External adapters are hard to test without a sandbox | Provider changes surface in production | Contract tests against provider sandboxes, or recorded responses | P2 | Provider sandbox credentials | No |
| TD-34 | ~~**Untested recovery paths in the outbox:** purge, lease expiry, two dispatchers racing~~ **Closed in M14.** Three integration tests in `NotificationTests`, and each was proven to fail against a deliberately broken processor before being kept: the purge test against a `DELETE` that forgot to check `ProcessedAt` (it would have taken unsent and dead rows with it), the lease test against a due query that ignored `LockedUntil`, and the race test against a `ClaimAsync` that stopped checking who won. The race is real rather than simulated — the first dispatcher is held inside the email provider (`Emails.Block()`) while holding the lease, so the window is wide open rather than hoped for — and its assertion is the product claim, not the mechanism: **the customer receives one email**. The lease test covers both halves, and the second is the one that matters: an expired lease must *release* the message, or a process that died holding one freezes it forever | Added with the happy path and the retry schedule | — | Done | Closed | None | No |
| TD-35 | **`MigrationRehearsalTests` is one large test** with seven phase-labelled blocks | Grew with each phase | A failure cannot be bisected | Split per phase | P3 | None | No |
| TD-36 | **Duplicated integration-test scaffolding**: the problem-details reader copied into twelve classes, four token parsers, two SQL counters | Copied as suites grew | Twelve places to change one helper | Move the shared pieces onto `TestApi` | P3 | None | No |
| TD-37 | **`TestCurrentUser.Customer(id)` uses the same id for the account and the customer**, which its own comment says are different concepts | Convenience | A handler that confuses the two passes every Application test | Use distinct ids and fix what fails | P2 | None | No |
| ~~TD-43~~ | **Closed in M18 — and the diagnosis in this row was wrong.** It read "the back-office journey fails on a busy database **without a product defect**" and prescribed a test-side fix (settle-then-click). M18 measured it instead: `RowActionsMenu` closed on *any* scroll event, and `html { scroll-behavior: smooth }` keeps a scroll firing for dozens of events after it starts — so a **keyboard** user who tabs to a row's ⋮ below the fold (the browser scrolls it into view) and presses Enter saw the menu open and vanish. Measured on a real stack: 32 scroll events, menu count 0 at 150 ms and after settling, while a row already in view opened normally. The control was unusable by keyboard on any row below the fold — a genuine accessibility defect, not a journey race | Found by M18's browser QA against the pipeline-deployed stack; the earlier reading came from Playwright's own auto-scroll, which made a real defect look like a test artifact | — | **Fixed in the component, not the journey:** the menu now repositions on scroll (rAF-throttled) instead of closing. A first attempt that closed when the trigger left the viewport reproduced the same bug from the other side — at open time the smooth scroll has only begun — and was rejected on measurement. `RowActionsMenu.test.jsx` locks it, mutation-tested against the original behaviour | — | — | Closed |
| ~~TD-56~~ | **Closed in M19.** The port is now derived from `SOUQ_E2E_BASE_URL` instead of hardcoded, and the fixture the file depends on is created by `scripts/qa-second-store.py` — idempotently, through the real API including the invitation flow, rather than "provision it once through the platform console" | — | — | What remains is **not** debt: `second.localhost` resolves only where Development host resolution is allowed, because in Production a store is reached through a registered, verified domain. That is the product behaving correctly, and `DeveloperQualityGates.md` now says so instead of leaving six red journeys to train readers to ignore red | — | — | Closed |
| TD-57 | **Browser journeys degrade as the QA store fills.** **Fixed in M19:** `back-office` (a product defect, TD-43); `experience` (asserting a feature that is off by default, while three siblings passed vacuously); `storefront-variants` (an ambiguous `role="status"` locator, a `settle()` helper awaiting animations with `iterations: Infinity`, and a navigation that cancelled an in-flight add); `admin-inventory`, partly (searching by Arabic name without pinning the interface language, and assuming page 1 of a screen ordered by lowest available stock). **M21 established the causes M20 recorded as unknown, and they were three different bugs, none of them accumulation itself — accumulation only made each one likely enough to see.** `admin-inventory`: the journey waited for the first `<tr>` before reading the page, and the loading skeleton **is** five real `<tr>` elements, so it read the skeleton, did not find its row, pressed Next and stepped over the page the row was on — then reported the row missing from the whole screen. `admin-search-insights`: the list is ordered by search volume, so a term searched once sinks as the log fills, and un-checking the filter left `keepPreviousData` showing the *previous* list with `aria-busy="false"` — busy is honest, it is answering a different question. `product-variants`: the success signal was a toast that was still on screen from the previous save, so the second save was never awaited and `page.reload()` cancelled the in-flight `PUT`. | The journeys share one seeded store and create data in it. By M21 that store held 275 inventory rows, 223 products, 263 customers, 87 orders and 1,097 search-log rows | **Measured, in order, on the same commit:** M19/M20 full serial runs passed 103 → 106 → 111 → 103, with a different two or three failing each time. M21 reproduced three of them **deterministically** on the grown store, fixed each at its cause, and re-ran each file three times in a row green. The product itself gained one fix from this: `DataTable` now carries `aria-busy`, because a loading table that a screen reader announces as five empty rows is not only a testing problem | **Each journey should own its store**, the way the integration suite does, instead of sharing the seeded one — that is what remains, and it is why this row stays open. Until then run with `--workers=1`. The three causes above are closed, so a fresh failure should be investigated on its own terms rather than attributed to this row | P2 | — | No |
| TD-58 | **A rule's named test may not assert the rule, and nothing checks it.** M19's coverage audit read all 229 rules in `BusinessRules.md` against the test bodies: every Arabic method name cited exists, but **89 rules name a test that asserts only part of what the rule states**, and 23 name no test at all — they cite a class, or nothing | `DocumentationTests`'s `Identifier` regex is `[A-Za-z_][A-Za-z0-9_]*`, which **cannot match an Arabic method name**, so the Tests column of every rule table is unchecked prose | The two documents that answer "how do we know this works?" can drift from the suites without any build failing — which is how five closed gaps stayed written as open until M19 | Generate the rule→test mapping and compare it, the way `GeneratedDocsTests` already does for `TestInventory.md`; at minimum fail the build when a cited test does not exist in the cited suite | P2 | — | No |
| TD-59 | **`BusinessRules.md` has no rule ids for four shipped capability areas**: catalog search (M3), search analytics (M13), reporting and dashboards (M12), and observability (M17). They are backed by roughly 125 test methods | The document was written before those areas shipped and nothing forces a rule row when a capability lands | "Every capability has a named test" cannot be claimed from the rule inventory while whole areas have no rule to name. `Traceability.md` gained rows for them in M19; `BusinessRules.md` has not | Give each area rule ids, then re-run the M19 audit against them | P2 | — | No |
| TD-60 | **None of the four rate limits is asserted, and the integration suite disables all of them.** `SouqApiFactory.cs:98` sets every `RateLimiting:{policy}:PermitLimit` to 100000 for the whole suite | The limits make the journeys and the suite flaky, so they were turned off wholesale rather than per-test | Nothing proves the buckets partition per host and per client address — which is the entire rationale, and the thing M15 found a real bypass in (host case). A regression would be invisible | Keep the suite's limits raised, and add a small set of tests that turn one policy down and assert the partitioning and the 429 | P1 | Security-relevant | No |
| TD-61 | **An archived store's endpoints are asserted by no test.** `TenantStatus.Archived` appears in the integration suite only in `BackgroundSweepScopeTests` and the factory; only `Suspended` availability is covered | Archiving arrived with platform provisioning and the availability tests were written against suspension | An archived store is an ended commercial relationship. One `switch` edit reopens its storefront with nothing failing | Mirror the suspended-store availability tests for archived | P1 | — | No |
| TD-62 | **"A store secret that cannot be decrypted fails loudly" is untested.** `PaymentGatewayUnavailableException` has **zero references in any test** | The path was written defensively and never exercised | If it ever silently falls through instead of throwing, a store's card payments are collected into the deployment's own account — the failure mode ADR-0031 exists to prevent | A test that corrupts a stored key and asserts the request fails rather than routing to the platform account | P0 | Money and tenant isolation | No |
| TD-63 | **Numeric security policies are asserted through their own constants.** Lockout threshold, lockout window, token lifetimes and the reset cooldown are each checked by referencing the constant that defines them, so `MaxFailedLogins = 500` is a green build | Convenient, and the constants read as the specification | The tests cannot catch a policy being loosened — only a mechanism being broken | Pin the advertised numbers as literals where a document or screen states them, as `PasswordRulesTests.الحدّان_هما_ما_تُعلنه_الواجهة` already does for 8/128 | P2 | Security-relevant | No |
| TD-55 | **A platform→tenant write and its audit row commit in two transactions.** `AuditBehavior` stages the row on the request's own `AppDbContext`, which is what makes "the audit commits with the change" true everywhere else. But `UpdateTenantPaymentAccountCommand`, `RemoveTenantPaymentAccountCommand` and `InviteTenantAdminCommand` perform their write inside `ITenantScopeRunner`, which opens a **new** scope with its own context — so the handler's save and the audit flush are separate. A crash between them leaves a store's payment account repointed, or a `TenantAdmin` invited, with no audit row | The scope runner exists so a platform action writes under the target store's tenant context; the audit row belongs to the platform area and is known at the outer layer | No record of three of the highest-value actions the platform can take, in a narrow failure window. Not attacker-triggerable on demand | Carry the staged entry into the inner scope so one save covers both — which requires deciding how a Platform-area row passes `TenantWriteGuardInterceptor`, so it is a design task, not a move | P2 | None | No. Found in M15; SEC-LOG-09 corrected from CURRENT to PARTIAL rather than left claiming a guarantee it does not give |
| TD-44 | **`PUT /api/admin/products/{id}/options` replaces the whole definition.** A form submitted from a stale read removes a value (or option) another admin added in between | The whole-definition contract keeps "one value per option per variant" atomic ([ADR-0040](../11-ADR/0040-product-option-model.md)). **Re-verified in M19, and the earlier note understated the work:** `GuardConcurrentEdit` only marks the root `Modified` so EF puts `RowVersion` in the `WHERE` clause — that catches two saves racing *within one window*, and cannot catch a form read an hour ago. **No product DTO exposes a version at all** (checked across the Products queries and the shared DTOs), so the remedy is not a handler change: it needs a version on the admin read model, threaded through the merchant options form, compared in the handler, plus tests on both sides | Rare lost edit of an unused value | Send the product's row version with the form and refuse a stale definition (`409`) — **a contract addition, not a patch** | Low | — | No. Reviewed in V3 and again in M19: neither phase touches the merchant options form, and the plan's own M19 rule is to close it only if the full-product pass makes fixing it natural. It does not, so it stays recorded with today's evidence rather than widening the certification phase |

## Documentation

| # | Debt | Why it exists | Impact | Fix | Priority |
|---|---|---|---|---|---|
| TD-38 | **Historical ADR statements have drifted from the code** (types renamed, phases shipped) | ADRs are historical records and are not rewritten | A reader of an old ADR can be misled | §4 of the [ADR index](../11-ADR/README.md) lists every drifted claim; keep it current when a decision is superseded | P3 |
| TD-39 | **Arabic code comments, English documentation** | The codebase was written in Arabic by its author | A non-Arabic-speaking team must translate comments that carry real reasoning | Decide the language policy for new comments; do not mass-translate | P3 |
| TD-40 | **Verified-dead code — the frontend and domain items are removed (Phase 16).** Deleted after re-confirming each by a repository-wide search: `HistoryIcon`, `ToastContext.info` and its CSS class, `CartContext.clear` with `api.clearBasket`, `api.deleteProduct`, the wishlist's unread `remove` and `onServer`, `CatalogTranslation.ToText()` and `CapturingEmailSender.Sent`. `api.getProductBySlug` is no longer dead — slug routing uses it. **Still open:** the three pieces of unused vocabulary that may be intentional (`ShippingMethod.FreeOver`, `StockMovementType.Return`, the type parameter of `BaseEntity<TId>`), which need a decision rather than a deletion | Written for a use that never arrived, or left behind when a feature moved | Each is a small false lead for a reader, and dead code is never covered by tests | Decide the three vocabulary items | P3 | A decision on the three | No |
